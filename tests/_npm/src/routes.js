//@ts-check

import { tokensToFunction } from "path-to-regexp";

// let a = null;

// import('path-to-regexp').then(exp => {
//     console.log(exp.compile)
//     a = exp.compile;
// })

let aaa = tokensToFunction;

export default aaa;
